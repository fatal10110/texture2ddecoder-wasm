{
  "name": "unity-asset-reader-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w unity-asset-reader -w unity-asset-reader-texture -w unity-asset-reader-node && npm run build:rollup -w texture2ddecoder-wasm",
    "test": "npm test -w unity-asset-reader -w unity-asset-reader-texture -w unity-asset-reader-node && node scripts/test-decoder.mjs && tsx --test scripts/tests/*.test.mjs scripts/tests/*.test.ts",
    "verify": "npm run build && npm test && npm run check:browser && npm run check:no-csharp",
    "build:wasm": "npm run build:wasm -w texture2ddecoder-wasm",
    "build:rollup": "npm run build:rollup -w texture2ddecoder-wasm",
    "check:browser": "node scripts/check-browser.mjs",
    "check:no-csharp": "node scripts/check-no-csharp.mjs"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.3.1",
    "@rollup/plugin-typescript": "^12.1.2",
    "@types/node": "^20.10.0",
    "esbuild": "^0.27.7",
    "rollup": "^4.30.1",
    "tslib": "^2.8.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
